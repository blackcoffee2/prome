# Project-root build entry. ESP-IDF discovers components under components/
# automatically; only the project name and the IDF bootstrap belong here.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(prome)